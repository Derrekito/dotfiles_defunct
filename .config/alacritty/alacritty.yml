# ~/.config/alacritty/alacritty.yml

shell:
  program: /usr/bin/tmux
  args:
    - new-session

clipboard:
  copy:
    format: JustText
  paste:
    shortcut:
      # Modify the key combination for pasting
      override: Control+Shift+V

render:
  gpu: true

dynamic_reload: true
